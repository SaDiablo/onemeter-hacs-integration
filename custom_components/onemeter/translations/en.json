{
  "config": {
    "step": {
      "user": {
        "title": "OneMeter Setup",
        "description": "Enter the API key and UUID for the device you want to fetch data for",
        "data": {
          "apikey": "API key",
          "device": "Device UUID"
        },
        "data_description": {
          "apikey": "API key",
          "device": "Device UUID"
        }
      }
    },
    "error": {
      "auth": "Can't load device data. Either the API key or device UUID is wrong"
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    }
  },
  "options": {
    "step": {
      "user": {
        "title": "OneMeter: Configuration",
        "data": {
          "sync_interval": "Sync Interval to Fetch Data in Seconds"
        }
      }
    }
  }
}
