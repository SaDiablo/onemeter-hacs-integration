name: Validate info.yml

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  validate:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v3"
      - name: Validate info.yml
        run: |
          echo "Brand check disabled for now - will be added later"
          # When ready to enable brand checks, uncomment the following:
          # python3 -m pip install git+https://github.com/home-assistant/brands
          # python3 -m pip install voluptuous
          # python3 -c "import voluptuous; import brands; brands.validate_all()"